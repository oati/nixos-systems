toolbar
- multi-account containers
- tab session manager
- dark background and light text
- ff2mpv
- passff

tab session manager:
- disable discarded property for lazy loading
- dark theme

multi-account containers:
- enable bookmark menus

cookie autodelete:
- enable automatic cleaning
- enable cache cleanup
- enable IndexedDB cleanup
- enable LocalStorage cleanup
- enable plugin data cleanup
- enable service workers cleanup
- disable cleanup log and counter
- disable show notification after automatic cleanup
- disable context menus

scroll anywhere:
- general
  - disable middle button
  - enable left button
  - scroll style grab and drag
- momentum
  - page weight 50 grams (default 1000)

dark background light text:
- set default method to invert

bypass paywalls clean:
- disable check update opt-in
- disable show options on update
- save
